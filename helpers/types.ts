export type University = {
  web_pages: string[];
  alpha_two_code: string;
  "state-province"?: string;
  domains: string[];
  name: string;
  country: string;
};

export type Country = {
  name: string;
  code: string;
};
